# webmcp-relay

Generic WebMCP relay: sessions, stateless-JWT auth, WebSocket back-channel,
tool-call routing, and mutation-confirmation protocol. No application-specific
imports. The host registers capabilities at startup via the registration
interface.
